package collision



var lEdge f64 // left 
var rEdge f64 // right 



func Setup () () {
	str.print("collision.Setup()")
}


func ColUpdate (ratio f32) () {
	//str.print("collision.ColUpdate()")

	lEdge = f32.f64(f32.mul(ratio, -1.0))
	rEdge = f32.f64(ratio)
}
package main

import "gl"
import "glfw"

import "app"
import "draw"
import "map"
import "mov"
import "space"
import "time"



func main () {
	gameInit()

	for bool.not(glfw.ShouldClose(app.Name)) {
		gameUpdate()
		drawAll()
		
		glfw.PollEvents()
		glfw.SwapBuffers(app.Name)
	}
}
